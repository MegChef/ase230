-- Simple Laravel database initialization
-- Just ensure the database exists - Laravel migrations will handle the rest
USE laravel;

-- Simple test to ensure database is ready
SELECT 'Database ready for Laravel!' as status;
